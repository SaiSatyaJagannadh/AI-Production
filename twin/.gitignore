.env*
# Secrets and build output
*.pem
*.key
*credentials*.json
secrets.json
__pycache__/
.venv/
venv/
node_modules/
.next/
out/

# Conversation transcripts written by the backend's local memory store —
# real chat content, must never be committed.
memory/
